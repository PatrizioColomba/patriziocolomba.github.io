import React, { FC } from 'react';


interface SnippetsProps {}

const Snippets: FC<SnippetsProps> = () => (
  <div data-testid="Snippets">
    Snippets Component
  </div>
);

export default Snippets;
